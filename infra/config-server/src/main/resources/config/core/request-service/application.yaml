spring:
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

  datasource:
    driverClassName: "org.postgresql.Driver"
    url: jdbc:postgresql://ewm-db:8032/requests_db}
    username: ${SPRING_DATASOURCE_USERNAME:main}
    password: ${SPRING_DATASOURCE_PASSWORD:main}

management:
  endpoints:
    web:
      exposure:
        include: info

server:
  port: 0

